// Edge Function `participation-respond` — Technical Blueprint V1.3 §13. Créatrice uniquement ; accept/decline, contrôle des places restantes.
import { notImplemented, serveFunction } from '../_shared/handler.ts';

serveFunction('participation-respond', { auth: 'user', requireAccountReady: false }, async () => {
  return notImplemented('participation-respond');
});

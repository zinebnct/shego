// Edge Function `plans-discover` — Technical Blueprint V1.3 §13. Enveloppe fn_discover_plans (§9) : fenêtres temporelles, places restantes, format de réponse Home/Carte.
import { notImplemented, serveFunction } from '../_shared/handler.ts';

serveFunction('plans-discover', { auth: 'user', requireAccountReady: false }, async () => {
  return notImplemented('plans-discover');
});
